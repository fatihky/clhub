- Make the entropy sources start running in load function (#678). It will make `get` run much faster when some time passes between calling `load()` and `get()`. See an example in [the quick start guide](https://github.com/fingerprintjs/fingerprintjs#quick-start).

The visitor identifiers are compatible with version 3.1.0.