**Features:**
- Custom `localeSubpaths` values (09be96f)

**Documentation:**
- Update `localeSubpaths` info (cfbda74)

**Bug Fixes:**
- Memory leak regression (cefb0f7)
- Add trailing slash to `ignoreRoutes` (a13e6f4)

**Refactor:**
- Use `window` instead of `process.browser` in `isServer`