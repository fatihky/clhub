**Bug Fixes:**
- Query string now preserved during language change on `localeSubpaths = true`

**DX:**
- Add error message if `wrappedComponentProps.pageProps` is undefined

**Features:**
- Add `customDetectors` support
- Refactor `appWithTranslation` to work with custom routes

