Chores:
- Updated deps
- Add GreenKeeper

Refactoring:
- Create `withConfig` HOC for internal use

Bug fixes:
- Fix double call of `getInitialProps`
- Use `url.format` for Link `as` formatting