Lots of bug fixes, including:
- Removal of `withNamespaces` props from `Link` component
- Use 302 redirects instead of 301 and prevent caching for `localeSubpaths`

New features:
- Add `serverLanguageDetection` config option
- Add error page to example