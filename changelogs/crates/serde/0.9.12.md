- Add `#[serde(into = "T")]` to serialize using the implementation of `Into<T>` and `#[serde(from = "T")]` to deserialize using the implementation of `From<T>` (#817, thanks @jbaublitz)

    ```rust
    #[derive(Serialize, Deserialize)]
    #[serde(into = "u32", from = "u32")]
    struct Ipv4Addr {
        /* ... */
    }
    ```

- Implement Deserialize for <code>Box&lt;<a href="https://doc.rust-lang.org/std/ffi/struct.CStr.html">CStr</a>&gt;</code> on nightly (#811, thanks @jonhoo)
- Implement Serialize and Deserialize for [`OsString`](https://doc.rust-lang.org/std/ffi/struct.OsString.html) and implement Serialize for [`OsStr`](https://doc.rust-lang.org/std/ffi/struct.OsStr.html) (#824, thanks @alexcrichton)
- Implement Serialize for [`Range`](https://doc.rust-lang.org/std/ops/struct.Range.html) (#813, thanks @rocallahan)