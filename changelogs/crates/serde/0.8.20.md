- Implement skip_deserializing for enum variants (#641, thanks @shinglyu)
  
  ``` rust
  #[derive(Deserialize)]
  enum E {
      A(bool),
      B(String),
      #[serde(skip_deserializing)]
      C(NotDeserializable),
  }
  ```
